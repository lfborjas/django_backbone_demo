#Backbone in django with piston 

A simple example of using django-piston to power a backbone app.

#Install

* Create a virtual environment
* virtualenv ..
* pip install -E .. -r requirements.txt
* ./manage.py syncdb
